# No environment variables are required for the current static deployment.
# Add project-specific variables here later only if the app gains backend
# integrations or other runtime configuration needs.
